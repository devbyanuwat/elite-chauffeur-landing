.env
.env.local
.DS_Store

# Astro
dist/
node_modules/
.astro/

# Blog — generated by BOS, served from shared volume in prod (not baked into image)
/blog/

# Editor + internal design references (local only)
.vscode/
refs/

# Session-pasted screenshots + local mockups/tooling (local only)
mqx*.png
.od-skills/
mockups/

# Serena MCP — project.yml is shared, the symbol cache and local overrides are not
.serena/cache/
.serena/project.local.yml
